POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=monolithe
POSTGRES_USER=postgres
# Use a real local password here; do NOT commit this file with real secrets.
POSTGRES_PASSWORD=your_local_postgres_password

AUDIT_POSTGRES_DB=audit

# Toggle security for local development
APP_SECURITY_ENABLED=false
KEYCLOAK_ISSUER_URL=http://localhost:8080/realms/sma-realm

EXTERNAL_SERVICE_URL=http://localhost:8091
AXON_AXONSERVER_SERVERS=localhost:8124

SERVER_PORT=8092
LOGGING_FILE_PATH=./backend/logs

# Copy this file to backend/.env and replace values with your local settings:
# cp backend/.env.example backend/.env
