# Sonar scanner project properties - ensure Sonar uses UTF-8
sonar.sourceEncoding=UTF-8
sonar.projectKey=christiandrochon_sma-eventsourcing-17
# sonar.host.url left to CI configuration (SonarCloud)
# Exclude binary assets and build directories from analysis to avoid encoding warnings
sonar.exclusions=**/static/img/**,**/target/**,**/frontend/target/**,**/frontend/frontend/logs/**,**/frontend/logs/**

