apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: pg-data-monolithe
  name: pg-data-monolithe
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
