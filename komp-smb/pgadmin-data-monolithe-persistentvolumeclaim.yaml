apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: pgadmin-data-monolithe
  name: pgadmin-data-monolithe
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
